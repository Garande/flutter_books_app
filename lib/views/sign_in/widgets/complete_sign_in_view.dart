import 'package:flutter/material.dart';

class CompleteSignInView extends StatelessWidget {
  const CompleteSignInView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}
